app:
  build: .
  links:
    - redis
  volumes:
    - .:/app
  environment:
    - REDIS_HOST=redis
    - REDIS_PORT=6379
    - REDIS_DB=1
  working_dir: /app
  ports:
    - "3000:3000"

redis:
  image: redis:2.8.19
